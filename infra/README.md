# infra

docker-compose and related infrastructure files (healthchecks, profiles, optional nginx).
Note: The only supported env file is the repo root `.env`. `infra/.env` is deprecated and not used by scripts.
