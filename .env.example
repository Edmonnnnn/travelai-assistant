# =====================
# MUST HAVE (local dev)
# =====================
POSTGRES_DB=travelai
POSTGRES_USER=travelai
POSTGRES_PASSWORD=change_me
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Base URL / CORS (defaults for local)
BASE_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# =====================
# OPTIONAL (degraded mode is allowed)
# =====================
# If empty: AI endpoints must respond "AI disabled: missing key"
OPENAI_API_KEY=

# Vector store (choose one). If empty/unreachable: vector features disabled gracefully
QDRANT_URL=
PGVECTOR_DSN=

# Optional cache
REDIS_URL=
